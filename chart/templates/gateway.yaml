apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: belajar-istio-gw
  labels:
    app: belajar-istio-gw
spec:
  selector:
    istio: ingress
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - 'istio.local'